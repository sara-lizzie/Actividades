function buscarMax(vector){
    let maximo=0;
    let v1 = 0;
    for(v1; v1 < vector.length; v1++){
        if(vector[i] > maximo){
            maximo = vector[i];
        }
    }
    return maximo;
}